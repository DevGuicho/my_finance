pre-commit:
  parallel: false
  commands:
    rubocop:
      run: bundle exec rubocop

    erb_lint:
      run: bundle exec erb_lint app/views app/components

    zeitwerk:
      run: bin/rails zeitwerk:check