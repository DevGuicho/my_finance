<%# Remove the modal from the DOM %>
<%= turbo_stream.update Category.new, "" %>

<%# Update the categories list %>
<%= turbo_stream.update "categories_list" do %>
  <%= render partial: "categories/category_list", locals: { categories: current_user.categories } %>
<% end %>

<%= turbo_stream.update "category_banner" do %>
  <%= render Categories::BannerComponent.new(categories: current_user.categories) %>
<% end %>

<%# Show flash notice %>
<%= turbo_stream.prepend "flash" do %>
  <div class="bg-green-50 border border-green-200 text-green-800 rounded-lg p-4 mb-4">
    Category was successfully created.
  </div>
<% end %>

